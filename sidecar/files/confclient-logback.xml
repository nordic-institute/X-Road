<?xml version="1.0" encoding="UTF-8" ?>
<configuration scan="true" scanPeriod="60 seconds">
    <property file="/etc/xroad/conf.d/variables-logback.properties" />
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d %level [xroad-confclient] [%thread] %logger{36} - %msg%n%rEx{3}</pattern>
        </encoder>
    </appender>

    <logger name="ee.ria.xroad" level="${XROAD_LOG_LEVEL}" />
    <logger name="akka" level="WARN" />
    <logger name="ee.ria.xroad.common.SystemPropertiesLoader" level="OFF" />

    <root level="${XROAD_LOG_LEVEL}">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
