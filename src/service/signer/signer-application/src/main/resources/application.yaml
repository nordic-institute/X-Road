quarkus:
  application:
    name: signer
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
      - xroad-common.yaml
      - xroad-logging.yaml
  scheduler:
    start-mode: forced
    enabled: true

"%containerized":
  xroad:
    signer:
      rpc:
        listen-address: 0.0.0.0

"%native":
  quarkus:
    log:
      file:
        enable: true
        path: /var/log/xroad/signer.log

"%cli":
  quarkus:
    scheduler:
      enabled: false
      start-mode: halted

"%test":
  quarkus:
    log:
      file:
        enable: false
  xroad:
    signer:
      rpc:
        enabled: false
