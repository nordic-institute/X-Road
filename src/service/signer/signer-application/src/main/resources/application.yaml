quarkus:
  application:
    name: signer
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
      - xroad-common.yaml
      - xroad-logging.yaml
  scheduler:
    start-mode: forced
    enabled: true
  log:
    category:
      "org.niis.xroad.signer.core.tokenmanager.TokenManager":
        level: "OFF"

"%containerized":
  xroad:
    signer:
      rpc:
        listen-address: 0.0.0.0

"%native":
  quarkus:
    log:
      file:
        enable: true
        path: /var/log/xroad/signer.log

"%ss":
  xroad:
    signer:
      ocsp-response-retrieval-active: true

"%test":
  quarkus:
    log:
      file:
        enable: false
  xroad:
    signer:
      rpc:
        enabled: false
