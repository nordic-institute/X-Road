quarkus:
  application:
    name: monitor
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
      - /etc/xroad/db.properties
      - xroad-common.yaml
      - xroad-logging.yaml

"%containerized":
  xroad:
    env-monitor:
      rpc:
        listen-address: 0.0.0.0
