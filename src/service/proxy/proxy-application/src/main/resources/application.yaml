quarkus:
  application:
    name: proxy
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
      - xroad-common.yaml
      - xroad-logging.yaml

"%containerized":
  xroad:
    proxy:
      rpc:
        listen-address: 0.0.0.0

"%native":
  quarkus:
    log:
      console:
        enable: false
      file:
        enable: true
        path: /var/log/xroad/proxy.log
#   TODO: clientproxy-access-log
#   TODO: serverproxy-access-log
#   TODO: and the rest logging configuration, check logback config

