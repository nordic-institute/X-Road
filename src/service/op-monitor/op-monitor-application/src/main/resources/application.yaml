quarkus:
  application:
    name: op-monitor-daemon
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
      - xroad-common.yaml
      - xroad-logging.yaml
  scheduler:
    start-mode: forced
    enabled: true

"%native":
  quarkus:
    log:
      file:
        enable: true
        path: /var/log/xroad/op-monitor.log
