config_ordinal: 275 # make local.yaml take precedence over other config files.

quarkus:
  config:
    locations:
      - /etc/xroad/conf.d/local.yaml
  log:
    file:
      enable: true
      path: /var/log/xroad/configuration_client.log
