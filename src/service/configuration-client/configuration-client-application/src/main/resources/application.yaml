quarkus:
  application:
    name: configuration-client
  config:
    locations:
      - xroad-common.yaml
      - xroad-logging.yaml
  scheduler:
    start-mode: forced
  quartz:
    thread-count: 3

"%containerized":
  xroad:
    configuration-client:
      rpc:
        listen-address: 0.0.0.0
