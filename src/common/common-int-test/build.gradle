plugins {
    id 'java-library'
}

dependencies {
    api project(':common:common-message')
    api project(':common:common-globalconf')
    api project(':signer-protocol')
    api project(':addons:hwtoken')

    api(libs.test.automation.core)
    api(libs.test.automation.allure)
    api(libs.test.automation.containers)
    api(libs.test.automation.feign)
    api(libs.test.automation.selenide) {
        exclude group: "org.slf4j", module: "*"
    }
    api(libs.bouncyCastle.bcpkix)
    api(libs.awaitility)
}

test {
    useJUnitPlatform()
}
