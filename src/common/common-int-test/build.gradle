plugins {
    id 'java-library'
}

dependencies {
    api project(':common:common-message')
    api project(':common:common-globalconf')
    api project(':signer-protocol')
    api project(':addons:hwtoken')

    api(libs.testAutomation.core)
    api(libs.testAutomation.allure)
    api(libs.testAutomation.containers)
    api(libs.testAutomation.feign)
    api(libs.testAutomation.selenide) {
        exclude group: "org.slf4j", module: "*"
    }
    api(libs.bouncyCastle.bcpkix)
    api(libs.awaitility)
}

test {
    useJUnitPlatform()
}
