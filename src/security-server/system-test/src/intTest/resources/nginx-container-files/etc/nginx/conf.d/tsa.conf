server {
    listen 8899 default_server;
    root /usr/share/nginx/html;
    index index.html index.htm;

    server_name cs;

    location / {
        limit_except GET POST {
            deny  all;
	}
        proxy_pass       http://ca:8899;
    }
}

