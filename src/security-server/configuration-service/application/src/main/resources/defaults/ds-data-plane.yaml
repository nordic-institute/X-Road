xroad:
  messagelog:
    hibernate:
      connection.username: "{vault}:messagelog.hibernate.connection#username"
      connection.password: "{vault}:messagelog.hibernate.connection#password"
  common:
    serverconf:
      hibernate:
        connection.username: "{vault}:serverconf.hibernate.connection#username"
        connection.password: "{vault}:serverconf.hibernate.connection#password"
edc:
  datasource:
    default:
      user: "{vault}:ds-data-plane.hibernate.connection#username"
      password: "{vault}:ds-data-plane.hibernate.connection#password"
