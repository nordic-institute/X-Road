xroad:
  common:
    serverconf:
      hibernate:
        connection.username: "{vault}:serverconf.hibernate.connection#username"
        connection.password: "{vault}:serverconf.hibernate.connection#password"
edc:
  datasource:
    default:
      user: "{vault}:edc-control-plane.hibernate.connection#username"
      password: "{vault}:edc-control-plane.hibernate.connection#password"
