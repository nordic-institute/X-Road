xroad:
  common:
    serverconf:
      hibernate:
        connection.username: "{vault}:serverconf.hibernate.connection#username"
        connection.password: "{vault}:serverconf.hibernate.connection#password"
edc:
  datasource:
    default:
      user: "{vault}:ds-identity-hub.hibernate.connection#username"
      password: "{vault}:ds-identity-hub.hibernate.connection#password"
