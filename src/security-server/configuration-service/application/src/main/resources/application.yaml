spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        native:
          searchLocations: file:///etc/xroad/conf.d/
  profiles:
    active: native
server:
  port: 5580

logging:
  level:
    ROOT: INFO
    org.springframework: INFO
