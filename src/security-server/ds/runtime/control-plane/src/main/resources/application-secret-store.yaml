spring:
  config:
    import:
      - vault://xrd-secret/ds-control-plane?prefix=secret-ds.

edc:
  datasource:
    default:
      user: "${secret-ds.username}"
      password: "${secret-ds.password}"
