spring:
  config:
    import:
      - vault://xrd-secret/ds-identity-hub?prefix=secret-ds.

edc:
  datasource:
    default:
      user: "${secret-ds#username}"
      password: "${secret-ds#password}"
