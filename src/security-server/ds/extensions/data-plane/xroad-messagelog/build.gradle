plugins {
    id("xroad.java-conventions")
}

dependencies {
    implementation libs.edc.core.connector

    implementation(project(":security-server:ds:spi:xroad-messagelog-spi"))

    implementation(project(":addons:messagelog:messagelog-addon:core"))
    implementation(project(":common:common-messagelog"))
    implementation(project(":lib:globalconf-model")) // for the GlobalConf

    implementation(project(":common:common-domain"))
    // for the ClientId, ServiceId identifiers
//    implementation(project(":serverconf"))

    testImplementation project(':common:common-keyconf')
    testImplementation libs.edc.junit
    testImplementation libs.assertj.core
    testImplementation libs.mockito.jupiter
}
