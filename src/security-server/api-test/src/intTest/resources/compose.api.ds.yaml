services:
  db-ds-control-plane:
    image: ${POSTGRES_DEV_IMG:-postgres-dev}
    pull_policy: always
    environment:
      - POSTGRES_PASSWORD=ds_control_plane_admin_secret
      - POSTGRES_USER=postgres
      - POSTGRES_DB=ds-control-plane
    networks:
      - xrd-ss-network
    ports:
      - "0:5432"
    healthcheck:
      start_period: 5s
      interval: 5s
      retries: 40
      test: [ "CMD", "pg_isready", "-U", "postgres", "-h", "localhost" ]
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M

  # The ds-* services read their serving certificate from OpenBao's tls/ds-https slot,
  # populated by SsBaselineSeeder through the admin API after the stack is up. Until
  # then they block at startup waiting for the slot, and nothing health-gates on them
  # before the seeder runs.
  ds-control-plane:
    image: ${DS_CONTROL_PLANE_IMG}
    pull_policy: always
    environment:
      - DEBUG=true
      - HOSTNAME=ds-control-plane
      - XROAD_HOST=ds-control-plane
      - XROAD_DSP_PARTICIPANT_CONTEXT_ID=ss0
      # Interpolated into edc.iam.trusted-issuer.issuer.id by the packaged application.yaml: the EDC
      # key is hyphenated, so an env var can only reach it through an X-Road key the yaml declares.
      - XROAD_EDC_IAM_TRUSTED_ISSUER_ISSUER_ID=did:web:ds-issuer-service%3A6183:issuer
      - XROAD_PROXY_DATAPLANE_URL=http://proxy:5590/full/api/v1/dataflows
      - XROAD_SECRET_STORE_HOST=openbao
      - XROAD_SECRET_STORE_TOKEN=${XROAD_SECRET_STORE_TOKEN}
      - XROAD_SECRET_STORE_SCHEME=http
      - XROAD_DB_DS_CONTROL_PLANE_HIBERNATE_CONNECTION_URL=jdbc:postgresql://db-ds-control-plane:5432/ds-control-plane
      - XROAD_DB_DS_CONTROL_PLANE_HIBERNATE_CONNECTION_USERNAME=postgres
      - XROAD_DB_DS_CONTROL_PLANE_HIBERNATE_CONNECTION_PASSWORD=ds_control_plane_admin_secret
      - XROAD_DB_SERVERCONF_HIBERNATE_CONNECTION_URL=jdbc:postgresql://db-serverconf:5432/serverconf
      - XROAD_DB_SERVERCONF_HIBERNATE_CONNECTION_DRIVER_CLASS=org.postgresql.Driver
      - XROAD_DB_SERVERCONF_HIBERNATE_CONNECTION_USERNAME=serverconf
      - XROAD_DB_SERVERCONF_HIBERNATE_CONNECTION_PASSWORD=secret
      # The registered-address form: counterparties derive this server's DSP endpoint from
      # the GlobalConf address (ds-gateway below forwards 8183 to this container).
      - EDC_DSP_CALLBACK_ADDRESS=https://ss0:8183/api/dsp/%s
      - DB_CONFIG_SOURCE_URL=jdbc:postgresql://db-serverconf:5432/serverconf
      - DB_CONFIG_SOURCE_USERNAME=serverconf
      - DB_CONFIG_SOURCE_PASSWORD=secret
    healthcheck:
      interval: 5s
      retries: 40
      test: [ "CMD", "curl", "-f", "-k", "http://localhost:4099/q/health" ]
    networks:
      - xrd-ss-network
    ports:
      - "0:8181"
      - "0:8183"
      - "0:8184"
      - "0:9999"
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 320M
    depends_on:
      db-ds-control-plane:
        condition: service_healthy
      configuration-client:
        condition: service_healthy
      # service_started, not service_healthy: identity-hub only becomes healthy once the
      # DS TLS slot is provisioned, which the seeder does after `docker compose up` returns.
      ds-identity-hub:
        condition: service_started

  db-ds-identity-hub:
    image: ${POSTGRES_DEV_IMG:-postgres-dev}
    pull_policy: always
    environment:
      - POSTGRES_PASSWORD=ds_identity_hub_admin_secret
      - POSTGRES_USER=postgres
      - POSTGRES_DB=ds-identity-hub
    networks:
      - xrd-ss-network
    ports:
      - "0:5432"
    healthcheck:
      start_period: 5s
      interval: 5s
      retries: 40
      test: [ "CMD", "pg_isready", "-U", "postgres", "-h", "localhost" ]
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M

  ds-identity-hub:
    image: ${DS_IDENTITY_HUB_IMG}
    pull_policy: always
    environment:
      - HOSTNAME=ds-identity-hub
      - XROAD_HOST=ds-identity-hub
      - EDC_IH_DID_PUBLIC_HOSTNAME=ss0
      - XROAD_SECRET_STORE_HOST=openbao
      - XROAD_SECRET_STORE_TOKEN=${XROAD_SECRET_STORE_TOKEN}
      - XROAD_SECRET_STORE_SCHEME=http
      - XROAD_DB_DS_IDENTITY_HUB_HIBERNATE_CONNECTION_URL=jdbc:postgresql://db-ds-identity-hub:5432/ds-identity-hub
      - XROAD_DB_DS_IDENTITY_HUB_HIBERNATE_CONNECTION_USERNAME=postgres
      - XROAD_DB_DS_IDENTITY_HUB_HIBERNATE_CONNECTION_PASSWORD=ds_identity_hub_admin_secret
      - DEBUG=true
    healthcheck:
      interval: 5s
      retries: 40
      test: [ "CMD", "curl", "-fk", "https://localhost:7181/api/check/health" ]
    networks:
      - xrd-ss-network
    ports:
      - "0:7183"
      - "0:7185"
      - "0:9999"
    depends_on:
      db-ds-identity-hub:
        condition: service_healthy

  db-ds-issuer-service:
    image: ${POSTGRES_DEV_IMG:-postgres-dev}
    pull_policy: always
    environment:
      - POSTGRES_PASSWORD=ds_issuer_service_admin_secret
      - POSTGRES_USER=postgres
      - POSTGRES_DB=ds-issuer-service
    networks:
      - xrd-ss-network
    ports:
      - "0:5432"
    healthcheck:
      start_period: 5s
      interval: 5s
      retries: 40
      test: [ "CMD", "pg_isready", "-U", "postgres", "-h", "localhost" ]
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 256M

  ds-issuer-service:
    image: ${DS_ISSUER_SERVICE_IMG}
    pull_policy: always
    environment:
      - HOSTNAME=ds-issuer-service
      - XROAD_HOST=ds-issuer-service
      - XROAD_SECRET_STORE_HOST=openbao
      - XROAD_SECRET_STORE_TOKEN=${XROAD_SECRET_STORE_TOKEN}
      - XROAD_SECRET_STORE_SCHEME=http
      - XROAD_DB_DS_ISSUER_SERVICE_HIBERNATE_CONNECTION_URL=jdbc:postgresql://db-ds-issuer-service:5432/ds-issuer-service
      - XROAD_DB_DS_ISSUER_SERVICE_HIBERNATE_CONNECTION_USERNAME=postgres
      - XROAD_DB_DS_ISSUER_SERVICE_HIBERNATE_CONNECTION_PASSWORD=ds_issuer_service_admin_secret
      - EDC_STATUSLIST_CALLBACK_ADDRESS=https://ds-issuer-service:6187/statuslist
      - DEBUG=true
    healthcheck:
      start_period: 60s
      interval: 5s
      retries: 40
      test: [ "CMD", "curl", "-fk", "https://localhost:6181/api/check/health" ]
    networks:
      - xrd-ss-network
    ports:
      - "0:6183"
      - "0:6185"
      - "0:6187"
      - "0:9999"
    deploy:
      resources:
        reservations:
          memory: 64M
        limits:
          memory: 320M
    depends_on:
      db-ds-issuer-service:
        condition: service_healthy

  # Owns this Security Server's GlobalConf-registered address (ss0) and fans its ports
  # out per backend: 5500/5577 to the proxy, 7183 to the identity hub (DID resolution),
  # 8183 to the control plane (DSP). Counterparties derive all of these from the one
  # registered address (XRDADR-41), so they must be served there.
  ds-gateway:
    image: nginxinc/nginx-unprivileged:stable-alpine
    volumes:
      - ./ds-gateway/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      xrd-ss-network:
        aliases:
          - ss0
    deploy:
      resources:
        reservations:
          memory: 16M
        limits:
          memory: 64M
    # service_started (not healthy): nginx only needs the backends' DNS entries to exist
    # when it resolves upstreams at startup.
    depends_on:
      proxy:
        condition: service_started
      ds-control-plane:
        condition: service_started
      ds-identity-hub:
        condition: service_started
