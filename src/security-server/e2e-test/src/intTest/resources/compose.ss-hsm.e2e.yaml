services:
  signer:
    build:
      context: ../../../development/docker/security-server/signer-with-hsm
      args:
        - REGISTRY_URL=${IMAGE_REGISTRY:-localhost:5555}
      dockerfile: Dockerfile
