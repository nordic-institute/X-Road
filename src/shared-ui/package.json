{
  "name": "@niis/shared-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix",
    "type-check": "vue-tsc --noEmit -p tsconfig.json --composite false",
    "build": "pnpm run generate-types",
    "generate-types": "rm -rf src/openapi-types && openapi-ts",
    "license-check": "license-check-and-add check -f license-check-and-add-config.json",
    "license-add": "license-check-and-add add -f license-check-and-add-config.json",
    "cleanup": "rm -rf dist && rm -rf node_modules",
    "format": "prettier .  --write"
  },
  "main": "./src/index.ts",
  "dependencies": {
    "@fontsource-variable/open-sans": "^5.2.7",
    "@fontsource-variable/material-symbols-rounded": "^5.2.44",
    "@vee-validate/i18n": "~4.15.1",
    "@vee-validate/rules": "~4.15.1",
    "axios": "~1.16.1",
    "dayjs": "^1.11.20",
    "deepmerge": "^4.3.1",
    "pinia": "^3.0.4",
    "pinia-plugin-persistedstate": "^4.7.1",
    "vee-validate": "^4.15.1",
    "vue": "^3.5.34",
    "vue-i18n": "^11.4.2",
    "vue-router": "^5.0.7",
    "vuetify": "^4.0.7"
  },
  "engines": {
    "npm": "use-pnpm",
    "yarn": "use-pnpm",
    "node": ">=22 <=24",
    "pnpm": ">=10"
  }
}
