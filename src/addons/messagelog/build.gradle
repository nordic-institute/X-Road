dependencies {
    compile project(':proxy')
    testCompile project(':common-test')
    testCompile 'org.hsqldb:hsqldb:2.3.2'
    testCompile "com.typesafe.akka:akka-testkit_2.11:$akkaVersion"
}

test {
    systemProperty "xroad.message-log.clean-interval", "0 0 0 1 1 ? 2099"
    systemProperty "xroad.message-log.archive-interval", "0 0 0 1 1 ? 2099"
}
