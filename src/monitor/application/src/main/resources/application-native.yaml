quarkus:
  package:
    output-directory: libs
    output-name: monitor-1.0
    jar:
      type: uber-jar
      add-runner-suffix: false

  log:
    console:
      enable: false
    file:
      enable: true
      path: /var/log/xroad/monitor.log

