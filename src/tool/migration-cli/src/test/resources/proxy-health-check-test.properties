# Test fixture for properties-path migration of value-conditional derived keys.
# proxy.health-check-port is non-zero, so the migration should also emit
# xroad.proxy.health-check-enabled=true.
proxy.health-check-port=5588
