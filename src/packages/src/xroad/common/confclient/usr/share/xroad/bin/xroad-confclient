#!/bin/bash
. /etc/xroad/services/confclient.conf
exec java ${XROAD_PARAMS} ${XROAD_CONFCLIENT_PARAMS} -cp "${CP}${ADDON_CP}" ${XROAD_ADDON_PARAMS} ee.ria.xroad.common.conf.globalconf.ConfigurationClientMain

