#!/bin/bash
. /etc/xroad/services/confclient.conf
exec java -XX:UseSVE=0 ${XROAD_PARAMS} ${XROAD_CONFCLIENT_PARAMS} -Dloader.path="${ADDON_CP}" ${XROAD_ADDON_PARAMS} -jar /usr/share/xroad/jlib/configuration-client.jar

