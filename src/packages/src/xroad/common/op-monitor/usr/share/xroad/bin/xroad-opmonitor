#!/bin/bash
. /etc/xroad/services/opmonitor.conf
exec java -XX:UseSVE=0 ${XROAD_PARAMS} ${XROAD_OPMON_PARAMS} -Dloader.path="${ADDON_CP}" ${XROAD_ADDON_PARAMS} -jar /usr/share/xroad/jlib/op-monitor-daemon.jar
