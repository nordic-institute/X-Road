#!/bin/bash
. /usr/share/debconf/confmodule

if [[ "$1" == "configure" || "$1" == "reconfigure" ]]; then
  RET=
  db_get xroad-common/database-host || RET=""
  db_stop

  /usr/share/xroad/scripts/setup_ds_catalogservice_db.sh "$RET"
fi

#DEBHELPER#

exit 0
