# Inventory for provisioning the native-lxd-stack on a Linux CI runner with a
# local LXD daemon (no Lima). Mirrors config/ansible_hosts.txt but the LXD host
# is the runner itself and the lxc remote is the local socket.

[cs_servers]
xrd-cs ansible_connection=lxd

[cp_servers]
#xrd-cp ansible_connection=lxd

[ca_servers]
xrd-ca ansible_connection=lxd ubuntu_releasever=noble

[ss_servers]
#xrd-ss0 ansible_connection=lxd
#xrd-ss1 ansible_connection=lxd

[ss_servers:children]
ubuntu_ss
rhel_ss

[ubuntu_ss:children]
ubuntu_hsm_ss

[ubuntu_hsm_ss]
xrd-ss1 ansible_connection=lxd

[rhel_ss]
xrd-ss0 ansible_connection=lxd

[is_servers]
xrd-is ansible_connection=lxd

[lxd_servers]
localhost ansible_connection=local

[compile_servers]
localhost ansible_connection=local

[hurl_servers]
xrd-hurl ansible_connection=lxd ubuntu_releasever=noble

[repo_servers]
xrd-repo ansible_connection=lxd

# Tracing container omitted on CI to keep the provisioning spike lean.
#[jaeger_servers]
#xrd-jaeger ansible_connection=lxd

### vars ###

[ss_servers:vars]
variant=vanilla

[all:vars]
ansible_lxd_remote=local
