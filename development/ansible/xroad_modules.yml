---
modules:
  signer:
    dir: signer
    build: build
    target: /usr/share/xroad/jlib/signer
    hosts: 
      - cp_servers
      - cs_servers
      - ss_servers
  signer-console:
    dir: signer-console
    build: build
    target: /usr/share/xroad/jlib/signer-console
    hosts: 
      - cp_servers
      - cs_servers
      - ss_servers

      
      
