---
# TEMPORARY (dataspaces dev): pre-install preseed runs before any xroad-*
# package starts a systemd unit. Puts the DS runtime configs on disk BEFORE the
# package postinst fires. Remove this include (and pre-ss-install.yml) once
# upstream packaging ships debconf preseeds for ds-* services and stops
# auto-starting units in postinst.
- include_tasks: pre-ss-install.yml

- include_tasks: install.yml

- include_tasks: postinstall.yml

- include_tasks: seed-config-properties.yml
