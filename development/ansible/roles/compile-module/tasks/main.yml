---
- name: Compile xroad modules {{selected_modules}}
  command: ../../scripts/compile-module.sh {{modules[item].dir}} {{modules[item].build}}
  args:
    chdir: ../../src
  loop: "{{selected_modules.split(',')}}"
