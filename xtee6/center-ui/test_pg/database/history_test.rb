require 'test_helper'

class HistoryTest < ActiveSupport::TestCase
  # FIXME: Because the history table is not mapped to the application code,
  # the tests must use raw SQL for examining the history table.
end
