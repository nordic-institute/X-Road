#!/bin/sh

if [ "$(id -nu)" != "sdsb" ]
then
echo this script must run under sdsb user
exit 1
fi

. /etc/sdsb/services/signer.conf

rlwrap ${JAVA_HOME}/bin/java ${SIGNER_PARAMS} ${SDSB_PARAMS} -jar /usr/share/sdsb/jlib/signer-console.jar $@
